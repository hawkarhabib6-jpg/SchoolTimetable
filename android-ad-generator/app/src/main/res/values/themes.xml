<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only covers the pre-Compose window frame. -->
    <style name="Theme.AdStudio" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#0B0B0F</item>
        <item name="android:navigationBarColor">#0B0B0F</item>
        <item name="android:windowBackground">#0B0B0F</item>
    </style>
</resources>
